# Minimal dependencies for the deployed Streamlit dashboard.
# The app (dashboard/app.py) imports only these - the training stack
# (DuckDB, Polars, LightGBM, scikit-learn) is NOT required at runtime.
#
# On Streamlit Community Cloud, point "Advanced settings -> Requirements file"
# at this file to get a faster, lighter build.
streamlit>=1.39
plotly>=5.24
pandas>=2.2
numpy>=1.26
pyarrow>=17.0
