# Optional slim dependency set for the deployed Streamlit dashboard.
# You do NOT need to reference this manually: dashboard/requirements.txt (next to
# app.py) is auto-detected by Streamlit Community Cloud. It is kept here as an
# explicit alternative if you prefer to point "Advanced settings -> Requirements
# file" at a root-level file.
streamlit>=1.49
plotly>=5.24
pandas>=2.2
numpy>=1.26
pyarrow>=17.0
